/**
 * Returns the external's filename
 * @param {string} externalName the external name
 * @returns {string}
 */
const getExternalFilename = (externalName) => `${externalName}.js`;

module.exports = getExternalFilename;
